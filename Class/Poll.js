class Poll {
    /**
     * Create a poll instance
     * @param {String} broadcaster_id User access token of the channel where post poll
     * @param {String} title Poll's title
     * @param {{title: String}[]} choices Choices list
     * @param {*} duration 
     * @param {*} channel_points 
     */
    constructor(broadcaster_id, title, choices, duration, channel_points) {
    }
}